server:
  port: ${SERVER_PORT:9000}
  servlet:
    session:
      cookie:
        name: AUTH_SESSION

spring:
  application:
    name: auth-server
    version: "@projectVersion@"

auth:
  token:
    # Runtime-specific per-client audience mappings are supplied by Compose or Kubernetes.
    client-audiences: { }
