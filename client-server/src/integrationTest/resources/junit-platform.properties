# JUnit creates test instances, so Spring does not automatically inject their single constructor as it does for production beans.
# Resolve every test-constructor parameter from the ApplicationContext without @Autowired, @Inject, or @TestConstructor.
# This does not enable component scanning; @SpringJUnitConfig, @SpringBootTest, or the test slice defines the context.
spring.test.constructor.autowire.mode=all
