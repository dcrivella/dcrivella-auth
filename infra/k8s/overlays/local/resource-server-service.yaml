apiVersion: v1
kind: Service
metadata:
  name: resource-server
  namespace: dcrivella-auth
spec:
  type: NodePort
  selector:
    app: resource-server
  ports:
    - name: http
      port: 8081
      targetPort: 8081
      nodePort: 30081
