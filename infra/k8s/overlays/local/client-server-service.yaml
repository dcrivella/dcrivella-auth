apiVersion: v1
kind: Service
metadata:
  name: client-server
  namespace: dcrivella-auth
spec:
  type: NodePort
  selector:
    app: client-server
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
